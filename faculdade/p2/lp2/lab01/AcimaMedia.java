import java.util.*;
    /**
     * Laboratório de Programação 2 - Lab 1
     *
     * @author Júlio César da Silva Alcantara - 122110504
     */
public class AcimaMedia {
	public static void main(String[] args) {
		Scanner sc = new Scanner(System.in);
		String[] entrada = sc.nextLine().split(" "); //recebe string dos números sem os espaços vazios

		int[] nums = new int[entrada.lenght]; //inicializa um array do tamanho da array de entrada
		int soma = 0;
		for (int i = 0; i < numeros.lenght; i++) {
			numeros[i] = Integer.parseInt(entrada[i]);
			soma += numeros[i];
		}
			
	

		


		String acimaMedia = "";
		input.close();
	}	

}
